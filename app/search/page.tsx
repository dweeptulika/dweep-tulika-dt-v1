import Link from "next/link"; import {liveArticles} from "@/lib/data";
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;const query=q.trim().toLowerCase();const items=query?liveArticles.filter(a=>`${a.title} ${a.description} ${a.author} ${a.labels.join(" ")}`.toLowerCase().includes(query)):[];return <div className="container"><h1 className="pageTitle">Search Dweep Tulika</h1><form className="search" action="/search"><input name="q" defaultValue={q} placeholder="Search stories…"/><button>Search</button></form>{query&&<p className="meta">{items.length} result(s)</p>}<div className="grid">{items.map(a=><article className="card" key={a.id}><div className="kicker">{a.labels[0]||"News"}</div><h2><Link href={a.filename}>{a.title}</Link></h2><p>{a.description}</p></article>)}</div></div>}